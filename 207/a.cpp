#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1e3 + 10;
int n, m, y, x[MAXN];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin >> n >> m >> y;
    for (int i = 0; i < n; i++) cin >> x[i];
    
    return 0;
}

/*
    
*/